diff --git a/src/streamvbyte_0124_decode.c b/src/streamvbyte_0124_decode.c
index 524cfe7..3d84f97 100644
--- a/src/streamvbyte_0124_decode.c
+++ b/src/streamvbyte_0124_decode.c
@@ -91,12 +91,13 @@ static const uint8_t *svb_decode_sse41_simple(uint32_t *out,
 
     int64_t Offset = -(int64_t)keybytes / 8 + 1;
 
-    const uint64_t *keyPtr64 = (const uint64_t *)keyPtr - Offset;
+    const uint8_t *keyBytes = keyPtr;
     uint64_t nextkeys;
-    memcpy(&nextkeys, keyPtr64 + Offset, sizeof(nextkeys));
+    memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
     for (; Offset != 0; ++Offset) {
       uint64_t keys = nextkeys;
-      memcpy(&nextkeys, keyPtr64 + Offset + 1, sizeof(nextkeys));
+      keyBytes += sizeof(nextkeys);
+      memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
 
       Data = svb_decode_sse41((keys & 0xFF), &dataPtr);
       svb_write_sse41(out, Data);
diff --git a/src/streamvbyte_x64_decode.c b/src/streamvbyte_x64_decode.c
index d079285..8471761 100644
--- a/src/streamvbyte_x64_decode.c
+++ b/src/streamvbyte_x64_decode.c
@@ -47,12 +47,13 @@ static inline const uint8_t *svb_decode_sse41_simple(uint32_t *out,
 
     int64_t Offset = -(int64_t)keybytes / 8 + 1;
 
-    const uint64_t *keyPtr64 = (const uint64_t *)keyPtr - Offset;
+    const uint8_t *keyBytes = keyPtr;
     uint64_t nextkeys;
-    memcpy(&nextkeys, keyPtr64 + Offset, sizeof(nextkeys));
+    memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
     for (; Offset != 0; ++Offset) {
       uint64_t keys = nextkeys;
-      memcpy(&nextkeys, keyPtr64 + Offset + 1, sizeof(nextkeys));
+      keyBytes += sizeof(nextkeys);
+      memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
 
       Data = svb_decode_sse41((keys & 0xFF), &dataPtr);
       svb_write_sse41(out, Data);
diff --git a/src/streamvbyte_x64_encode.c b/src/streamvbyte_x64_encode.c
index 0c4dfb7..df6b73e 100644
--- a/src/streamvbyte_x64_encode.c
+++ b/src/streamvbyte_x64_encode.c
@@ -77,7 +77,8 @@ static size_t streamvbyte_encode_SSE41 (const uint32_t* in, uint32_t count, uint
 		_mm_storeu_si128((__m128i *)dataPtr, r1);
 		dataPtr += len_lut[keys >> 8];
 
-		*((uint16_t*)keyPtr) = (uint16_t)keys;
+		const uint16_t packedKeys = (uint16_t)keys;
+		memcpy(keyPtr, &packedKeys, sizeof(packedKeys));
 		keyPtr += 2;
 	}
 
@@ -88,7 +89,7 @@ static size_t streamvbyte_encode_SSE41 (const uint32_t* in, uint32_t count, uint
 		uint32_t dw = in[i];
 		uint32_t symbol = (dw > 0x000000FF) + (dw > 0x0000FFFF) + (dw > 0x00FFFFFF);
 		key |= symbol << (i + i);
-		*((uint32_t*)dataPtr) = dw;
+		memcpy(dataPtr, &dw, sizeof(dw));
 		dataPtr += 1 + symbol;
 	}
 	memcpy(keyPtr, &key, ((count & 7) + 3) >> 2);
diff --git a/src/streamvbytedelta_x64_decode.c b/src/streamvbytedelta_x64_decode.c
index 4cf85e7..6072fdb 100644
--- a/src/streamvbytedelta_x64_decode.c
+++ b/src/streamvbytedelta_x64_decode.c
@@ -77,12 +77,13 @@ static const uint8_t *svb_decode_sse41_d1_init(uint32_t *out,
 
     int64_t Offset = -(int64_t)keybytes / 8 + 1;
 
-    const uint64_t *keyPtr64 = (const uint64_t *)keyPtr - Offset;
+    const uint8_t *keyBytes = keyPtr;
     uint64_t nextkeys;
-    memcpy(&nextkeys, keyPtr64 + Offset, sizeof(nextkeys));
+    memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
     for (; Offset != 0; ++Offset) {
       uint64_t keys = nextkeys;
-      memcpy(&nextkeys, keyPtr64 + Offset + 1, sizeof(nextkeys));
+      keyBytes += sizeof(nextkeys);
+      memcpy(&nextkeys, keyBytes, sizeof(nextkeys));
       // faster 16-bit delta since we only have 8-bit values
       if (!keys) { // 32 1-byte ints in a row
 
diff --git a/src/streamvbytedelta_x64_encode.c b/src/streamvbytedelta_x64_encode.c
index cdc399c..496be72 100644
--- a/src/streamvbytedelta_x64_encode.c
+++ b/src/streamvbytedelta_x64_encode.c
@@ -55,7 +55,8 @@ static size_t streamvbyte_encode_SSE41_d1_init (const uint32_t* in, uint32_t cou
 		_mm_storeu_si128((__m128i *)dataPtr, r1);
 		dataPtr += len_lut[keys >> 8];
 
-		*((uint16_t*)keyPtr) = (uint16_t)keys;
+		const uint16_t packedKeys = (uint16_t)keys;
+		memcpy(keyPtr, &packedKeys, sizeof(packedKeys));
 		keyPtr += 2;
 	}
   prev = (uint32_t)_mm_extract_epi32(Prev,3);
@@ -66,7 +67,7 @@ static size_t streamvbyte_encode_SSE41_d1_init (const uint32_t* in, uint32_t cou
 		uint32_t dw = in[i] - prev; prev = in[i];
 		uint32_t symbol = (dw > 0x000000FF) + (dw > 0x0000FFFF) + (dw > 0x00FFFFFF);
 		key |= symbol << (i + i);
-		*((uint32_t*)dataPtr) = dw;
+		memcpy(dataPtr, &dw, sizeof(dw));
 		dataPtr += 1 + symbol;
 	}
 	memcpy(keyPtr, &key, ((count & 7) + 3) >> 2);
