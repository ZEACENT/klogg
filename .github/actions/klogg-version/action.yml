name: "Prepare klogg version env var"
description: ""
runs:
  using: "composite"
  steps:
    - name: Set klogg version
      shell: sh
      run: |
        echo "KLOGG_VERSION=25.11.12.$((${{ github.run_number }} + 717))" >> $GITHUB_ENV 
